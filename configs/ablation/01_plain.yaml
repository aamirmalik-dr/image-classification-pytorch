# Ablation baseline: the small CNN with no regularization at all.
name: plain
model:
  arch: cnn
  batch_norm: false
  dropout: 0.0
train:
  epochs: 10
  lr: 0.001
  weight_decay: 0.0
  batch_size: 128
  seed: 0
