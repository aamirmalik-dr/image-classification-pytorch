# Ablation: weight decay only, isolating its effect.
name: weight_decay
model:
  arch: cnn
  batch_norm: false
  dropout: 0.0
train:
  epochs: 10
  lr: 0.001
  weight_decay: 0.0005
  batch_size: 128
  seed: 0
