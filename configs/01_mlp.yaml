# Flatten-then-dense baseline. Ignores spatial structure, so it sets the floor
# the convolutional models are measured against.
name: mlp
model:
  arch: mlp
train:
  epochs: 10
  lr: 0.001
  weight_decay: 0.0
  batch_size: 128
  seed: 0
