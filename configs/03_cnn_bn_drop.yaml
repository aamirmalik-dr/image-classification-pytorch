# The same CNN with batch norm and dropout, plus weight decay. This is the
# regularized convolutional model.
name: cnn_bn_drop
model:
  arch: cnn
  batch_norm: true
  dropout: 0.3
train:
  epochs: 10
  lr: 0.001
  weight_decay: 0.0005
  batch_size: 128
  seed: 0
